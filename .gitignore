# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# Build outputs
dist/
build/
out/
*.tsbuildinfo

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Environment (NEVER commit these with real keys!)
.env
.env.*
!.env.example
!.env.*.example
!.env.sample
# Explicit env files in subdirectories (defense in depth)
**/.env
**/.env.local
**/.env.production
**/.env.development
openclawd-stack/bridge/.env
openclawd-stack/template/.env
openclawd-stack/orchestrator/.env
websocket-server/.env
x-bot/.env
llm-wiki-tang/.env
llm-wiki-tang/web/.env.local

# NPM tokens / auth
.npmrc
!scripts/.npmrc.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Testing
coverage/
.nyc_output/

# Temporary
tmp/
temp/
*.tmp

# Secrets (never commit these)
*.pem
*.key
secrets/
credentials/

# Backup directories (keep empty dirs structure)
tailclawd-backup/

# Build / cache artifacts that may contain bundled secrets from prior builds
**/.next/
**/.netlify/
**/.vercel/
**/.venv/
**/.cache/
**/dist-cache/
*.pyc
__pycache__/

# Go module cache (contains thousands of test PEM files)
solana-clawd/.cache/

# Editor/shell history
.zsh_history
.bash_history

# Backup files
*.bak
*.orig
*~
