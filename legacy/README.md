# legacy/

Archived variants of `clawd-code-*`. Not part of the active monorepo.

The canonical CLI is [`../clawd-code-cli/`](../clawd-code-cli/) — the onboarding flow points there.

| Dir | What it was | Status |
|---|---|---|
| `clawd-code-localy/` | Local-only launcher / proxy scripts, docs-only | Archived |
| `clawd-code-main/` | Fork of `@anthropic-ai/claude-code` upstream | Archived — reference only |
| `clawd-code-proxy-main/` | Python proxy variant (not Node) | Archived |

If you need to resurrect one of these, move it out of `legacy/` and update [`../ONBOARDING.md`](../ONBOARDING.md) to document which is canonical.
